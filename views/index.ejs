<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles.css">
    <title>Marvel Quotes App</title>
</head>
<body>
    <h1>We're in the endgame now!</h1>
    <form action="/quotes" method="POST">
        <input type="text" placeholder="name" name="name">
        <input type="text" placeholder="quote" name="quote">
        <button type="submit">Submit</button>
    </form>
    <div>
        <h2>I am inevitable!</h2>
        <p>
          Replace first Cap's quote with a quote by Thanos
        </p>
        <button id="update-button">Replace quote</button>
    </div>
    <div>
        <h2>Remove Thanos!</h2>
        <p>
          Delete one Thanos's quote. Does nothing if there are none.
        </p>
        <button id="delete-button">Delete quote</button>
    </div>
    <div id="message"></div>
    <h2>Quotes</h2>
    <ul class="quotes">
        <% for(let i=0;i<quotes.length;i++){ %>
        <li class="quote">
            <span><%=quotes[i].name %></span>:
            <span><%=quotes[i].quote %></span>
        </li>
        <% } %>
    </ul>
    <script src="main.js"></script>
</body>
</html>